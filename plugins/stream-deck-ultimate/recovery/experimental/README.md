# Unshipped profile repair experiments

The local acceptance workspace also contained `build_profile_pack.py` and `fix_profile_pages.py`. They attempted to repair the D1 page-ID warning. The obvious ID rewrite caused total profile import failure on real Stream Deck software, so these experiments are intentionally not part of the canonical build. Preserve them only for investigation; do not run them on a release without new physical proof.

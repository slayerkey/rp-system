# Market Command Center 1.0.2 QA

## 1.0.3 real-iCUE import recovery

- Customer report: iCUE rejected the 1.0.2 package as unsupported/corrupted with \`JSON parsing error in meta parameters\`.
- Root cause: the \`x-icue-groups\` \`application/json\` metadata contained a raw ampersand in \`S&P 500\`. The grouping JSON is valid JSON, but this metadata lives inside iCUE's strict-XML \`<head>\` import path.
- Fix: encode the ampersand as the JSON escape \`S\u0026P 500\`, which becomes \`S&P 500\` after JSON parsing without leaving a raw \`&\` in the metadata source.
- Shared regression: \`tools/xeneon/inline.py\` now validates every \`application/json\` metadata block as both valid JSON and XML-safe text before generating a shipping widget.
- Release version advanced to 1.0.3 so the repaired package can be published as a distinct revision.


## Update scope

- Migrated the published widget into the canonical RatPack XENEON source layout.
- Added `Show Major Markets`, pinning SPY, QQQ, and DIA at the top as clearly labelled S&P 500, Nasdaq 100, and Dow proxies.
- Changed the visual setting label from `Show Sparklines` to `Show Charts` while preserving the existing property key for compatibility.
- Added persistent rolling stock/ETF sparklines from successful Finnhub quote observations.
- Kept the honest session low/high range as the fallback until at least two stock quote samples exist.
- Fixed chart toggle behavior so disabling charts hides both sparklines and range bars.
- Aligned the refresh control with the runtime's real 15 minute minimum.
- Combined stock and crypto stale state instead of only reporting crypto staleness.
- Updated iCUE property reads to use the canonical static direct-binding bridge generated by `tools/xeneon/inline.py`.

## Local smoke completed

- Source split and inline rebuild completed successfully.
- JavaScript syntax checks passed.
- Runtime smoke with mocked Finnhub quotes passed for:
  - major markets shown first
  - custom stock shown
  - day-range fallback on first sample
  - sparkline after the second observed quote
  - `Show Charts` off behavior
  - `Show Major Markets` off behavior
- `.icuewidget` ZIP integrity passed with root `index.html` and `manifest.json`.

## Remaining canonical gate

Run the repository XENEON workflow for `market-command-center` so the official CORSAIR CLI, lexical iCUE settings smoke, Corsair Labs runner, and StreamSpell packaged preview validate the GitHub source.
